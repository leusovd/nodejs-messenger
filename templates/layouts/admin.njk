<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }}</title>

        {# Site Css #}
        {% include 'partials/siteCss.njk' %}
        {% block pageCss %}{% endblock %}
    </head>

    <body class="body{{ ' -' ~ active }}{% if pageName %}{{ ' -' ~ pageName }}{% endif %}">

        {# Header #}
        {% include 'partials/header.njk' %}

        <div class="panel">
            <div class="container">
                <div class="row no-gutters">
                    <div class="col">
                        <div class="panel-content">
                            {% block content %}{% endblock %}
                        </div>
                    </div>
                </div>

            </div>
        </div>

        {# Site Js #}
        {% include 'partials/siteJs.njk' %}
        {% block pageJs %}{%endblock%}
        </body>
    </html>